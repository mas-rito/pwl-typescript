function greet(name: string, umur: number): string {
    return `Selamat datang, ${name} di PWL, umur saya ${umur}`;
}

const mahasiswa = "Budi";
const umur : number = 19
console.log(greet(mahasiswa, umur));
